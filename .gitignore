__pycache__/
*.py[cod]
.venv/
.repoglass/
build/
dist/
*.egg-info/
.pytest_cache/

# Generated by `rpg init`, and it pins every resolved value, so a
# committed copy stops tracking changes to the defaults.
/repoglass.toml

# The local eval set names files inside private repositories. The
# corpus benchmark (`run_corpus.py`) uses semble's public data and
# needs nothing from here.
/benchmarks/queries/
/benchmarks/.bench/
